// Name:
// Date:
// What this program does:
//
// The same combat score as Battle Sim, but the formula lives in a method
// so you write it once instead of twice.

import java.util.Scanner;

public class BattleScore {
    public static void main(String[] args) {
        Scanner input = new Scanner(System.in);

        // Ask for fighter 1's three stats, then call warriorScore.
        // Ask for fighter 2's three stats, then call it again.
        // Print both scores.

    }

    // Returns (2 * strength + armor + stamina) / 4.0
    public static double warriorScore(int strength, int armor, int stamina) {
        return 0.0;
    }
}
